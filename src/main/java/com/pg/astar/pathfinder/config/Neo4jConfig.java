package com.pg.astar.pathfinder.config;

import org.springframework.context.annotation.Configuration;

/** Configuration for Neo4j to ensure relationships are loaded properly. */
@Configuration
public class Neo4jConfig {

  // No additional configuration needed here for now
  // Spring Data Neo4j will use default settings
}
